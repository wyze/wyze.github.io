// @flow

import StackOverflow from '../../../../src/shared/components/svgs/StackOverflow'
import snapshot from '../../../helpers/snapshot'

describe('<StackOverflow />', () => {
  it('renders', snapshot.bind(null, StackOverflow))
})
