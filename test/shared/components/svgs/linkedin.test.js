// @flow

import LinkedIn from '../../../../src/shared/components/svgs/LinkedIn'
import snapshot from '../../../helpers/snapshot'

describe('<LinkedIn />', () => {
  it('renders', snapshot.bind(null, LinkedIn))
})
