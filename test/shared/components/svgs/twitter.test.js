// @flow

import Twitter from '../../../../src/shared/components/svgs/Twitter'
import snapshot from '../../../helpers/snapshot'

describe('<Twitter />', () => {
  it('renders', snapshot.bind(null, Twitter))
})
