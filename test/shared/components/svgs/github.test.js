// @flow

import GitHub from '../../../../src/shared/components/svgs/GitHub'
import snapshot from '../../../helpers/snapshot'

describe('<GitHub />', () => {
  it('renders', snapshot.bind(null, GitHub))
})
