export const level = {
  alignItems: 'center',
  display: 'flex',
  justifyContent: 'space-between',
}

export const makeRGBA = (alpha: number) => `rgba(17, 17, 17, ${alpha})`

export const thin = {
  fontWeight: 300,
}
